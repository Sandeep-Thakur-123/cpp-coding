#include <iostream>
using namespace std;

int main() {
    string s = "123 456";

    string temp = "";

    for(char c : s)
    {
        if(c != ' ')
            temp += c;
    }

    int num = stoi(temp);

    cout << num;
}


/// using (stoi) string to integer function

int main(){
stirng s = "123";
int num = stoi(s);
cout << num;

/// if we have alphabets in integers

#include <iostream>
#include <string>
using namespace std;

int main() {
    string s = "1213wewr456";
    string temp = "";

    for(char c : s)
    {
        if(c >= '0' && c <= '9')
        {
            temp += c;
        }
    }

    int num = stoi(temp);

    cout << num;
}

/// if we have both space and alphabets


